{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml"
  xmlns:news="http://www.google.com/schemas/sitemap-news/0.9">
  {{ with .GetPage "/posts" }}
    {{ $two_days_ago_date := now.AddDate 0 0 -2 }}
    {{ range (where .Pages "Date" "gt" $two_days_ago_date) }}
      {{ $post := . }}
      <!-- Write only html format  -->
      {{ with .OutputFormats.Get "html" }}
        <url>
          <loc>{{ .Permalink }}</loc>
          {{ if not $post.Lastmod.IsZero }}
            <lastmod>{{ $post.Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>
          {{ end }}
          <changefreq>hourly</changefreq>
          <news:news>
            <news:publication>
              <news:name>{{ $.Site.Title }}</news:name>
              <news:language>{{ $.Site.Language }}</news:language>
            </news:publication>
            <news:publication_date>{{ $post.Date.Format "2006-01-02T15:04:05-07:00" }}</news:publication_date>
            <news:title>{{ $post.Title }}</news:title>
          </news:news>
        </url>
      {{ end }}
    {{ end }}
  {{ end }}
</urlset>
